export type profileType = {
    address: {
        city: string,
        street:string
    },
    name: {
        firstname: string,
        lastname: string
    }
    email: string,
    phone: string
}